# Write a Python program that asks the user to input a string and counts the number of characters (including spaces) in that string.

# Step 1: Ask the user to input a string
user_string = "Python ID - Online Python Compiler"

# Step 2: Count the number of characters
length = len(user_string)

# Step 3: Display the result
print(f"The number of characters in the string is: {length}")
